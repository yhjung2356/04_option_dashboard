# 🚀 실시간 반영 가이드 (CI/CD)

## 📋 목차
1. [빠른 시작 (추천)](#빠른-시작)
2. [방법 비교](#방법-비교)
3. [핫 리로드 (개발용)](#핫-리로드)
4. [자동 배포 스크립트](#자동-배포-스크립트)
5. [Docker 자동 배포](#docker-자동-배포)
6. [GitHub Actions (최고 추천)](#github-actions)
7. [Jenkins 설정](#jenkins-설정)
8. [AWS 자동 배포](#aws-자동-배포)

---

## 🎯 빠른 시작 (개발 중 실시간 반영)

### 방법 1: 핫 리로드 (가장 빠름!) ⭐
```bash
# 파일 변경 시 자동으로 재시작
hot-reload.bat
```
- ✅ **Spring Boot DevTools** 내장
- ✅ 파일 저장하면 **자동으로 재시작**
- ✅ 코드/HTML/CSS/JS 모두 적용
- ⚡ **가장 빠른 개발 방법**

### 방법 2: 자동 배포 스크립트
```bash
# Git Pull → 빌드 → 재시작 (한 번에!)
auto-deploy.bat
```
- ✅ Git에서 최신 코드 받아옴
- ✅ 자동 빌드
- ✅ 기존 프로세스 종료 후 재시작

### 방법 3: Docker 자동 배포
```bash
# Docker로 완벽한 환경 일관성
docker-auto-deploy.bat
```
- ✅ Git Pull → Maven 빌드 → Docker 빌드 → 실행
- ✅ 환경 일관성 보장

---

## 📊 방법 비교

| 방법 | 속도 | 난이도 | 자동화 | 추천 상황 |
|------|------|--------|--------|----------|
| **핫 리로드** | ⚡⚡⚡ | ⭐ | 파일 저장 시 | 개발 중 |
| **자동 배포 스크립트** | ⚡⚡ | ⭐ | 클릭 한 번 | 로컬 테스트 |
| **Docker** | ⚡ | ⭐⭐ | 클릭 한 번 | 프로덕션 환경 |
| **GitHub Actions** | ⚡⚡ | ⭐⭐ | Git Push 시 | 팀 협업 |
| **Jenkins** | ⚡⚡ | ⭐⭐⭐⭐ | Git Push 시 | 대규모 프로젝트 |

---

## ⚡ 핫 리로드 (개발용)

### 실행 방법
```bash
# 방법 1: 스크립트 실행
hot-reload.bat

# 방법 2: 직접 명령어
mvnw spring-boot:run
```

### 작동 원리
1. **Spring Boot DevTools** 활성화
2. 파일 변경 감지 (클래스, HTML, CSS, JS)
3. 자동으로 애플리케이션 재시작
4. **브라우저 새로고침만 하면 끝!**

### 확인 방법
```
파일 수정 → 저장 (Ctrl+S)
→ 콘솔에 "Restarting..." 메시지
→ 브라우저 새로고침 (F5)
→ 변경사항 적용됨!
```

### 제외할 파일 (선택사항)
`application.properties`에 추가:
```properties
# DevTools 설정
spring.devtools.restart.enabled=true
spring.devtools.restart.exclude=static/**,public/**,templates/**
spring.devtools.livereload.enabled=true
```

---

## 🔄 자동 배포 스크립트

### auto-deploy.bat (로컬)
```bash
# 실행
auto-deploy.bat
```

**동작:**
1. ✅ Git Pull (최신 코드)
2. ✅ 기존 프로세스 종료
3. ✅ Maven 빌드
4. ✅ 애플리케이션 실행
5. ✅ 브라우저 자동 열림

**사용 시나리오:**
- Git에 코드 푸시됨
- 로컬에서 최신 버전 테스트하고 싶을 때
- `auto-deploy.bat` 실행 한 번으로 끝!

---

## 🐳 Docker 자동 배포

### docker-auto-deploy.bat
```bash
# 실행
docker-auto-deploy.bat
```

**동작:**
1. ✅ Git Pull
2. ✅ 기존 컨테이너 중지
3. ✅ Maven 빌드
4. ✅ Docker 이미지 빌드
5. ✅ 컨테이너 실행

**장점:**
- 환경 일관성 보장
- 프로덕션과 동일한 환경
- 쉬운 롤백

**명령어:**
```bash
# 로그 확인
docker-compose logs -f

# 중지
docker-compose down

# 재시작
docker-compose restart
```

---

## 🌟 GitHub Actions (최고 추천!)

### 설정 완료됨!
`.github/workflows/ci-cd.yml` 파일이 생성되어 있습니다.

### 작동 방식
```
코드 수정 → Git Push
    ↓
GitHub Actions 자동 실행
    ↓
1. 빌드 (Maven)
2. 테스트
3. Docker 이미지 생성
4. (선택) AWS 배포
    ↓
완료! 🎉
```

### 초기 설정

#### 1. GitHub Secrets 설정
```
Settings → Secrets and variables → Actions
```

**필수 Secrets:**
- `DOCKER_USERNAME`: Docker Hub 사용자명
- `DOCKER_PASSWORD`: Docker Hub 비밀번호

**AWS 배포 시 추가:**
- `EC2_HOST`: EC2 인스턴스 IP
- `EC2_USER`: SSH 사용자명 (보통 ec2-user)
- `EC2_SSH_KEY`: SSH 개인키

#### 2. 사용 방법
```bash
# 코드 수정 후
git add .
git commit -m "기능 추가"
git push origin main

# GitHub Actions 자동 실행!
# https://github.com/your-repo/actions 에서 진행상황 확인
```

### 결과
- ✅ **자동 빌드**
- ✅ **자동 테스트**
- ✅ **Docker 이미지 자동 생성**
- ✅ **(선택) EC2 자동 배포**

---

## 🔧 Jenkins 설정

### Jenkins vs GitHub Actions

| 항목 | Jenkins | GitHub Actions |
|------|---------|----------------|
| **설정 난이도** | ⭐⭐⭐⭐ | ⭐⭐ |
| **유지보수** | 어려움 | 쉬움 |
| **비용** | 서버 필요 | 무료 |
| **추천도** | 대규모 조직 | 중소규모 |

### Jenkins 설치 (Windows)

#### 1. Jenkins 다운로드
```
https://www.jenkins.io/download/
```

#### 2. 설치 및 실행
```bash
# Jenkins 설치 후
# http://localhost:8080 접속
```

#### 3. 초기 설정
1. 초기 암호 입력 (콘솔에 표시됨)
2. "Install suggested plugins"
3. Admin 계정 생성

#### 4. 플러그인 설치
```
Jenkins → Manage Jenkins → Plugins
```
필요한 플러그인:
- Git plugin
- Maven Integration plugin
- Pipeline plugin
- GitHub plugin

#### 5. Job 생성

**Freestyle Project:**
```
1. New Item → Freestyle project
2. Source Code Management: Git
   Repository URL: [your-repo-url]
3. Build Triggers:
   ☑ GitHub hook trigger for GITScm polling
4. Build:
   Execute Windows batch command:
   ```
   call mvnw clean package -DskipTests
   ```
5. Post-build Actions:
   Execute Windows batch command:
   ```
   call auto-deploy.bat
   ```
```

**Pipeline (권장):**
```groovy
// Jenkinsfile
pipeline {
    agent any
    
    tools {
        maven 'Maven 3.9'
        jdk 'JDK 17'
    }
    
    stages {
        stage('Checkout') {
            steps {
                git 'your-repo-url'
            }
        }
        
        stage('Build') {
            steps {
                bat 'mvnw clean package -DskipTests'
            }
        }
        
        stage('Deploy') {
            steps {
                bat 'auto-deploy.bat'
            }
        }
    }
    
    post {
        success {
            echo 'Build and Deploy Successful!'
        }
        failure {
            echo 'Build or Deploy Failed!'
        }
    }
}
```

### GitHub Webhook 설정
```
GitHub Repo → Settings → Webhooks → Add webhook
Payload URL: http://your-jenkins-server:8080/github-webhook/
```

---

## ☁️ AWS 자동 배포

### EC2 인스턴스 준비

#### 1. 보안 그룹 설정
- 포트 8080 (애플리케이션)
- 포트 22 (SSH)

#### 2. EC2 접속
```bash
ssh -i your-key.pem ec2-user@your-ec2-ip
```

#### 3. Docker 설치
```bash
sudo yum update -y
sudo yum install docker -y
sudo systemctl start docker
sudo usermod -a -G docker ec2-user

# Docker Compose 설치
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

#### 4. 프로젝트 클론
```bash
git clone your-repo-url
cd futures-options-dashboard
```

#### 5. 자동 배포 설정
```bash
# crontab으로 주기적 자동 배포 (선택)
crontab -e

# 매일 새벽 3시 자동 배포
0 3 * * * cd /home/ec2-user/futures-dashboard && git pull && docker-compose down && docker-compose up -d
```

### GitHub Actions와 연동
- 위에서 설정한 `.github/workflows/ci-cd.yml`의 deploy 섹션 활성화
- GitHub Secrets에 EC2 정보 입력
- Git Push하면 자동으로 EC2에 배포됨!

---

## 💡 추천 워크플로우

### 개발 단계
```bash
# 1. 핫 리로드로 빠르게 개발
hot-reload.bat

# 파일 수정 → 저장 → 새로고침
# 반복...
```

### 테스트 단계
```bash
# 2. 로컬에서 완전한 빌드 테스트
auto-deploy.bat
```

### 배포 단계
```bash
# 3. Git Push로 자동 배포
git add .
git commit -m "기능 완성"
git push origin main

# GitHub Actions가 자동으로:
# - 빌드
# - 테스트
# - Docker 이미지 생성
# - (선택) EC2 배포
```

---

## 🎯 실제 사용 예시

### 시나리오: UI 수정
```bash
# 1. hot-reload.bat 실행 (한 번만)
hot-reload.bat

# 2. dashboard.html 수정
# 3. Ctrl+S 저장
# 4. 브라우저 F5 새로고침
# → 바로 반영됨!
```

### 시나리오: Java 코드 수정
```bash
# 1. hot-reload.bat 실행 중
# 2. Service 클래스 수정
# 3. Ctrl+S 저장
# 4. 콘솔에 "Restarting..." 표시
# 5. 브라우저 F5
# → 새 코드 실행됨!
```

### 시나리오: 팀원에게 공유
```bash
# 1. 코드 완성
git add .
git commit -m "새 기능 추가"
git push origin main

# 2. GitHub Actions 자동 실행 (2-3분)
# 3. 팀원이 EC2에서 자동으로 최신 버전 사용
```

---

## ❓ FAQ

### Q1: 가장 빠른 방법은?
**A:** 개발 중에는 `hot-reload.bat`가 최고!

### Q2: Jenkins vs GitHub Actions?
**A:** 
- **중소규모**: GitHub Actions (쉽고 무료)
- **대규모/복잡한 파이프라인**: Jenkins

### Q3: Tomcat은 필요 없나요?
**A:** Spring Boot는 **내장 Tomcat**을 사용합니다. 별도 Tomcat 설치 불필요!

### Q4: Docker를 꼭 써야 하나요?
**A:** 아니요! 하지만 Docker 사용 시:
- ✅ 환경 일관성
- ✅ 쉬운 배포
- ✅ 격리된 실행

### Q5: 비용은?
- **핫 리로드**: 무료
- **GitHub Actions**: Public repo 무료, Private는 월 2000분 무료
- **Jenkins**: 서버 비용 필요
- **Docker**: 무료

---

## 🎉 결론

### 추천하는 최종 구성

**개발 중:**
```bash
hot-reload.bat  # ← 가장 빠름!
```

**로컬 테스트:**
```bash
auto-deploy.bat  # ← 간편함!
```

**프로덕션 배포:**
```bash
git push  # ← GitHub Actions 자동 실행!
```

**이미 모든 스크립트가 준비되어 있습니다!** 🚀

---

**작성일**: 2025-12-21
**버전**: 1.0.0
